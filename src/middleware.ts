// Este arquivo não é mais necessário pois o app usará apenas Português
// e não necessita de middleware para internacionalização.
// Por favor, delete este arquivo do seu projeto.

export default function middleware() {}

export const config = {
  matcher: ['/((?!api|_next/static|_next/image|assets|favicon.ico|icon.svg).*)']
};
