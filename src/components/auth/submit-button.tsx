// src/components/auth/submit-button.tsx
"use client";

// Este componente não é mais necessário, pois a lógica de submissão
// agora está contida diretamente nos formulários de login e signup
// com um estado de `isSubmitting` local.
// Pode ser removido para simplificar o projeto.
export {};
