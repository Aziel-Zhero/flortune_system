
// Este arquivo foi esvaziado pois a autenticação foi desativada temporariamente.
// Manter o arquivo evita erros de build se alguma importação residual ainda existir.
export const { handlers: { GET, POST }, auth, signIn, signOut } = { handlers: {}, auth: null, signIn: null, signOut: null };
